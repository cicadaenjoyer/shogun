# web/ - the Shogun web app

The main app people use in a browser, built mobile-first so it also works well
on phones. TypeScript + React + Vite, hosted on Firebase Hosting.

This is a single pnpm workspace package: `package.json` lives directly here.
